# // https://docs.aws.amazon.com/lambda/latest/dg/images-create.html
# 
# 1  yar start
# 2  yarn start
# 3  apt-get install libatk-bridge2.0-0
# 4  sudo apt-get install libatk-bridge2.0-0
# 5  yarn start
# 6  sudo apt-get install -yq --no-install-recommends libasound2 libatk1.0-0 libc6 libcairo2 libcups2 libdbus-1-3 libexpat1 libfontconfig1 libgcc1 libgconf-2-4 libgdk-pixbuf2.0-0 libglib2.0-0 libgtk-3-0 libnspr4 libpango-1.0-0 libpangocairo-1.0-0 libstdc++6 libx11-6 libx11-xcb1 libxcb1 libxcursor1 libxdamage1 libxext6 libxfixes3 libxi6 libxrandr2 libxrender1 libxss1 libxtst6 libnss3
# 7  yarn start
# 8  lsb -a
# 9  lsb
# 10  lbs
# 11  lsb_release -d
# 12  wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
# 13  sudo dpkg -i google-chrome-stable_current_amd64.deb
# 14  sudo apt-get -f install
# 15  sudo dpkg -i google-chrome-stable_current_amd64.deb
# 16  yarn start
# 17  reset
# 18  history
# 19  history > history.txt
